
# Separate prod project (its own GitHub repo) — intentionally excluded
q-trust-saas/

# Safety net (each sub-app also declares its own .gitignore)
node_modules/

# Generated knowledge graph (~7MB, rebuilt by /graphify) — derived data, not source
graphify-out/
